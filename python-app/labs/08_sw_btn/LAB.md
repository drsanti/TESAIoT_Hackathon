# Lab 08 — SW buttons

Stream button state / press counts.

```bash
python labs/08_sw_btn/lab.py              # on_change — press buttons
python labs/08_sw_btn/lab.py --periodic   # unattended smoke
```

**Next:** Lab 09 (dashboard).
