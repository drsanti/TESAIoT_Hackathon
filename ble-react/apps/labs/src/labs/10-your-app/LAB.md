# Lab 10 — Your scaffold

## Goal

Choose a sensor set and run a minimal app using the same session hooks as `apps/dashboard`.

## On-screen steps

1. Connect.
2. Toggle sensors → **Go live + apply selected**.

## Acceptance

Selection applied and at least one selected sensor publishes → chapter complete.

## Notes

Graduate with `pnpm dev:dashboard` → `http://localhost:5175`.
